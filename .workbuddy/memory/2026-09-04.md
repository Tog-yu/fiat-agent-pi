# 2026-09-04

## Stage 7 完成：Pi 本地源码解耦 → registry 0.80.3

用 auto-coder skill 继续 Stage 7（P7-29~P7-33），全部翻 [x]，进度 34/54。

### 改动
- **P7-29** `package.json`：4 条 `file:../pi/packages/*` → 精确 `0.80.3`（不含 `^`），保留 `pi-agent-core`(驱动循环) 与 `pi-tui`(TUI 入口)。
- **P7-30** `tsconfig.json` / `tsconfig.runtime.json`：移除指向本地 Pi 的 8 条 `paths`，类型检查改走 node_modules 正常解析。
- **P7-31** 新增 `scripts/switch-pi-deps.mjs` + `dev:pi-local` / `dev:pi-registry` npm script，一键在「本地源码链接」与「registry 0.80.3」间切换（已实测双向可用）。
- **P7-33** `DEV_SPEC.md` §9 新增「版本纪律」：钉版本→跑测试→再 bump，Pi 版本 bump 为独立任务，禁止顺手升级（呼应 §2.5 决策①）。

### 关键踩坑（重要）
- `npm install` 不会自动替换已满足版本的本地符号链接：lockfile 仍记 `link:true + resolved:../pi/packages/*`，npm 忠实重建 symlink，导致**假解耦**。
- 正确做法：手术式删除 lockfile 中 4 条 `node_modules/@earendil-works/pi-*` 的 `link` 记录 + `rm -rf node_modules/@earendil-works`，再 `npm install`，才真正从 registry 下载为真实目录（无 symlink）。
- 解耦后 `test/smoke.test.ts` 编译报错：`@earendil-works/pi-coding-agent/core/skills.ts` 是未导出子路径（registry 包 exports 仅 `.` 与 `./rpc-entry`）。`loadSkills` 已在主入口 re-export，改为从主入口导入并去掉 `.ts` 扩展名。这正符合「只依赖公开 API」的解耦目标。

### 回归
- `npm run check` 全绿（biome + tsgo），`npm test` 125/125 通过、16/16 文件绿。
- 已与 `../pi` 彻底解耦：node_modules 下 4 个 pi 包均为真实目录，无符号链接。

### 提交
- 本地 commit `225e94e`（13 文件，+6379/-2885）。按 iron rules 未 push，待用户显式触发。

### 下一步（Stage 8）
- P8-34 最小循环：用 `Agent` / `runAgentLoop` 驱动「问一句答一句」，对标 openclaw `pi-embedded` 最小骨架。

## Stage 8 推进：P8-34 最小内嵌循环（commit `31ba124`，未 push）

用户要求先 push 再继续，但 push 被 SSH 认证硬挡（`id_ed25519` 受口令保护、agent 无密钥、未存 macOS 钥匙串；agent shell 无法交互输入口令）。用户选择「先继续 Stage 8」，push 稍后处理。

### P8-34 实现
- 新增 `src/server/host/loop.ts`：`PiHostLoop` 用 `pi-agent-core` 的 `Agent` 直驱最小循环，对标 openclaw `pi-embedded` 骨架。
- **关键 API 面（以 0.80.3 d.ts 为准，未照搬 0.75.5）**：
  - `Agent` 构造：`new Agent({ streamFn, sessionId, initialState: { model, systemPrompt, messages } })`。注意 `AgentState.model` 是必填，必须写进 `initialState`（Agent 选项本身没有 `model` 字段）。
  - `streamFn: (model, context, options?) => streamSimple(model, context, { ...options, apiKey })`，取 `StreamFn` 签名（`model: Model<Api>, context: Context, options?: SimpleStreamOptions`）。`streamSimple` 来自 `@earendil-works/pi-ai/compat`。
  - auth 透传：`SimpleStreamOptions extends StreamOptions` 带 `apiKey?: string`，由 `getApiKey(provider)` 解析后填入（不依赖全局 AuthStorage，比 createAgentSession 更轻）。
  - 跑一轮：`await agent.prompt(text)` → 读 `agent.state.messages` 取最后 assistant 文本。
  - 工具挂载点预留：`agent.state.tools = [...]`（P8-36 L1b）。最小循环不挂工具。
- 新增 `test/host-loop.test.ts`：`registerFauxProvider` 离线验证单轮 + 多轮「问一句答一句」（2 测试）。
- 回归：`npm run check` 全绿；全量 **127/127** 通过（+2）。进度 35/54，下一任务 P8-35（会话基础设施：SessionManager / parseSessionEntries / migrateSessionEntries / CURRENT_SESSION_VERSION / DefaultResourceLoader / SettingsManager）。

### push 阻塞（待用户处理）
- 远程 `git@github.com:Tog-yu/fiat-agent-pi.git`（SSH）。`git push --ff-only` 是**错误用法**（push 不支持 --ff-only，那是 pull/merge 的 flag）——之前误判为沙箱杀，实际是 unknown option 退出 129。
- 真正卡点：SSH key 口令，agent shell 无法输入。需用户在自己终端 `ssh-add ~/.ssh/id_ed25519`（同 macOS 会话 launchd agent 被本 shell 共享）后，由我 `git push origin main`。两笔本地提交 `225e94e` + `31ba124` 待 push。

## P8-34 完成：pi-host 最小内嵌循环（commit 31ba124，进度 35/54）

- 新增 `src/server/host/loop.ts`：`PiHostLoop` 用 `pi-agent-core` 的 `Agent` 直驱，不依赖 `createAgentSession`/扩展加载器。
- API 要点（0.80.3 d.ts 实测）：`Agent.prompt` 返回 `Promise<void>`（不是 event stream）；`AgentState.model` 必填须写进 `initialState`；`AgentState.messages: AgentMessage[]`（`AgentMessage = Message | CustomAgentMessages[...]`）。
- `streamFn = (model, ctx, opts) => streamSimple(model, ctx, {...opts, apiKey})`，`streamSimple` 来自 `pi-ai/compat`，`StreamOptions.apiKey` 透传。
- 测试用 `registerFauxProvider` + **`fauxAssistantMessage("...")`**（不是 `{type:"text",text}`，`FauxResponseStep` 类型不接受后者）。
- 全量 127/127 通过（+2）。

## P8-35 完成：会话基础设施接入（commit 671f265，进度 36/54）

- 新增 `src/server/host/session.ts`：`HostSession` 薄包装 `SessionManager`（create/open/continueRecent/inMemory）；`messages()` = `buildSessionContext().messages`（`AgentMessage[]` 与 `AgentState.messages` 类型直通）；`syncDelta()` 每轮增量落盘；静态 `parseAndMigrate` 显式接入 `parseSessionEntries` + `migrateSessionEntries` + `CURRENT_SESSION_VERSION`。
- 新增 `src/server/host/resources.ts`：`HostResources` 包装 `SettingsManager.inMemory` + `DefaultResourceLoader`（noExtensions/noSkills/noPromptTemplates/noThemes/noContextFiles: true，保留 `extensionFactories` L1a 通道）。
- `loop.ts` 接入：构造恢复历史 transcript、runTurn 后落盘、systemPrompt 优先取 resources。
- **关键踩坑**：`DefaultResourceLoader.getSystemPrompt()` 返回的是**从文件发现**的系统提示词，构造传入的 `systemPrompt` 选项**不回显**（noContextFiles 下为空）——宿主必须自己存注入值，`HostResources.systemPrompt = injected ?? loader.getSystemPrompt()`。
- 包未导出 `Settings` 类型 → 用 `Parameters<typeof SettingsManager.inMemory>[0]` 推断。
- 测试 6 用例离线全过；全量 **133/133**（+6）。`Settings`/`fauxAssistantMessage` 的类型坑都实测踩过并记录。

### 遗留
- Push 阻塞：`id_ed25519` 受口令保护且未入钥匙串，本地 3 笔提交（225e94e / 31ba124 / 671f265）待用户 `ssh-add` 后推送。
- 下一任务 P8-36（L1b 工具注册通道）。

## P8-36 完成：工具注册通道 L1b（commit 3eee910，进度 37/54）

- 新增 `src/server/host/tools.ts`：`defineHostTool`（无 ExtensionAPI 的 L1b 契约雏形，双泛型 `<TParameters extends TSchema, TDetails>` 透传保留 execute params 推断）+ `registerTools`（写 `agent.state.tools`，覆盖式替换整组）。
- `loop.ts` tools 类型收紧为 `HostTool[]`（移除 `Array<unknown>` + `as never`）。
- **类型事实（0.80.3 实测）**：
  - `AgentTool<TParameters extends TSchema, TDetails> = Tool + label + execute(toolCallId, params, signal?, onUpdate?) => AgentToolResult{content, details, terminate?}` + 可选 `prepareArguments`/`executionMode`。
  - `StopReason = "stop"|"length"|"toolUse"|"error"|"aborted"`；faux 用 `stopReason:"toolUse"` 触发工具循环。
  - `TSchema` 来自 **`typebox`@1.1.38**（不是 @sinclair/typebox；pi 传递依赖被 npm 提升到顶层，可直接 import）。
  - `HostTool = AgentTool<any>` 需 biome-ignore（单行紧贴上一行，跨行不生效）；对齐 pi 的 `AgentState.tools: AgentTool<any>[]` 口径。
  - **泛型陷阱**：`defineHostTool<T extends HostTool>` 恒等写法会把字面量收窄到默认 TSchema → `Static<TSchema>=unknown`，execute params 丢类型；必须双泛型透传。
- faux 全链路离线测试法：`faux.setResponses([fauxAssistantMessage([fauxToolCall(name, args)], {stopReason:"toolUse"}), (ctx)=>{...断言 ctx.tools/ctx.messages...; return fauxAssistantMessage("...") }])`——第 2 步 factory 的 `ctx` 能断言 provider 视角的 tools 与 toolResult 回灌。
- 全量 **137/137**（+4）。下一任务 P8-37（L1a extensionFactories 钩子通道）。

## P8-37 完成：钩子通道 L1a（commit c40ab15，进度 38/54）

- 新增 `src/server/host/embedded-factories.ts`：`buildEmbeddedExtensionFactories` 白名单入口（对标 openclaw 同名函数）。
- 新增 `src/server/host/extensions.ts`：
  - `setupEmbeddedExtensions`：DefaultResourceLoader（no* 全 true + extensionFactories 注入）→ `loader.reload()` → `ExtensionRunner(extensions, runtime, cwd, SessionManager.inMemory, ModelRegistry.inMemory(AuthStorage.inMemory()))`。**实测无需 bindCore 即可 emitToolCall**（钩子不碰 ctx.actions 时）。
  - `bridgeAgentHooks`：extension `tool_call`/`tool_result` 钩子 → Agent options `beforeToolCall`/`afterToolCall`。**形状直通**：`ToolCallEventResult{block,reason}` ≡ `BeforeToolCallResult`；`ToolResultEventResult{content,details,isError}` ⊆ `AfterToolCallResult`。event 构造用 `CustomToolCallEvent`（宿主工具均自定义名）；`input` 传 validated args（可变引用，钩子就地改参）。
- loop.ts 透传 `beforeToolCall`/`afterToolCall`。
- **实测语义**：被 block 的工具**不发 tool_result 事件**（错误 tool result 由 Agent 循环直接生成，不走 afterToolCall）——audit-hook 对 block 工具的审计要靠 tool_call 钩子自身记录。
- **关键类型事实**：`AgentToolCall` = content block `{type:"toolCall", id, name, arguments}`；`ExtensionHandler<E,R> = (event, ctx) => Promise<R|void>|R|void`；`ExtensionRunner` / `createExtensionRuntime` / `loadExtensionFromFactory` 均从 pi-coding-agent 包根导出。
- 闸门② 全链路离线验证通过；全量 **139/139**（+2）。`before_agent_start` 桥接（model-router 用）留 P8-38。
- 下一任务 P8-38（宿主职责移植）。

## P8-38 完成：宿主职责移植（commit 5f05815，进度 39/54）

- 新增 `src/server/host/duties.ts`：`sanitizeMessages`（空消息丢弃/连续重复去重键序无关/stripImages·stripThinking，默认保留 thinking·图片）、`buildBootstrapContext`（user role 自述来源）、`fanoutEvents`（单订阅多播 + 错误隔离）。
- loop.ts 接线（全用 Pi 官方挂载点）：sanitize → Agent `transformContext`（只洗 LLM 视图不动 transcript）；bootstrap → 构造时追加在恢复历史后（新 session 随首轮 syncDelta 落盘，resume 不重复）；`runTurnSafe` → catch prompt 抛错 + 检查 assistant `stopReason:"error"` 的 `errorMessage`。
- **实测**：faux `fauxAssistantMessage("...", {stopReason:"error", errorMessage})` 会让 prompt **正常返回不抛**——兜底必须双查（catch + stopReason），单靠 try/catch 漏 provider 错误。
- 全量 **147/147**（+8）。下一任务 P8-39（版本差异对齐落表）/ P8-40（关闭自动发现自测）。

## P8-39 完成：版本差异对齐落表（commit f9d8276，进度 40/54）

- 发现本地就有 openclaw 源码：`~/Desktop/project/openclaw`（四包钉 0.75.5），对照不再靠 spec 转述，全部一手实测。
- 12 行差异表落 DEV_SPEC §9（版本纪律之后）。关键发现：
  1. **OpenClaw 全仓零 `ExtensionRunner` import**——它的 tool-result 桥以 factory 形式交给 `createAgentSession` 内部跑；我们直驱路线必须显式建 Runner（架构必差异）。
  2. OpenClaw 驱动以 `createAgentSession` 高层 SDK 为主（attempt.ts:2746），Agent/runAgentLoop 只少量 import——我们 Agent 直驱是有意分歧，非 0.75.5→0.80.3 变化。
  3. `DefaultResourceLoader` no* 选项 + extensionFactories 两版同名同形全兼容；`ExtensionFactory` 签名一致。
  4. OpenClaw 把 tool_result rawEvent 当 unknown 防御式解析；0.80.3 有强类型 ToolResultEvent。
- 升级 Pi 版本时先重跑此表核对（faux/typebox/Settings 导出口径）。
- 下一任务 P8-40（关闭自动发现 + 通道自测）——阶段 8 收官。
